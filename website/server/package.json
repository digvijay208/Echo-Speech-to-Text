{
  "name": "echo-site-server",
  "private": true,
  "version": "0.1.0",
  "description": "Express server for the Echo marketing site — serves the built frontend + handles the waitlist + app metadata.",
  "type": "module",
  "main": "index.js",
  "scripts": {
    "start": "node index.js",
    "dev": "node --watch index.js"
  },
  "dependencies": {
    "cors": "^2.8.5",
    "express": "^4.21.2",
    "express-rate-limit": "^7.5.0"
  }
}
